#!/usr/bin/env python
import roslib;roslib.load_manifest('hpp_node')

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("urdfDescription", str_t, 0, "Autogenerated parameter based on model.", "")
gen.add("srdfDescription", str_t, 0, "Autogenerated parameter based on model.", "")

exit(gen.generate("hpp_node", "hpp_node", "hpp"))
